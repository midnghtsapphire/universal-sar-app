DETAILED ERROR ANALYSIS
=======================
The operation CREATE succeeds (returns id 60001), but then the SUBJECT INSERT fails.

The error is in the subjects table insert. Looking at the SQL:
insert into `subjects` (`id`, `operationId`, `subjectType`, `subjectSubtype`, `name`, `description`, `photoUrl`, `lastKnownLat`, `lastKnownLng`, `lastKnownAlt`, `lastSeenAt`, `directionOfTravel`, `circumstances`, `status`, `attributes`, `createdBy`, `createdAt`, `updatedAt`) 
values (default, ?, ?, ?, ?, default, default, ?, ?, default, ?, default, ?, default, ?, ?, default, default)

params: 60001,human,hiker,Ryan Shtuka,50.8812,-119.8925,2026-04-02 21:31:16.584,...

The issue: operationId is 60001 but the operation was just created. The operation ID might actually be different. Let me check the createOperation function to see what ID it returns.

Actually, looking more carefully at the params: the first param is 60001 which maps to operationId. The operation was created successfully and returned id 60001. But the subject insert is failing.

The error is "Failed query" which means the SQL itself failed. This could be:
1. A column type mismatch
2. The `description` is being set to `default` but the column might not have a default
3. The `attributes` JSON column might not accept the stringified JSON

Wait - looking at the SQL: `description` is `default` (not NULL). The column in schema is `text("description")` which should allow NULL. But drizzle might be sending `default` when the value is undefined. Need to check if the column has a default value or if we need to explicitly pass NULL.

The fix: In the createSubject call in routers.ts, we need to ensure undefined values are converted to null, not left as undefined (which drizzle converts to `default`).
